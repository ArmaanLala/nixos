#cloud-config
users:
  - name: armaan
    uid: 1000
    groups: [sudo]
    shell: /bin/bash
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    lock_passwd: false
    plain_text_passwd: pwn
    ssh_authorized_keys:
      - "@SSH_PUBKEY@"
ssh_pwauth: false
package_update: true
package_upgrade: true
apt:
  conf: |
    Acquire::Retries "5";
bootcmd:
  - dpkg --add-architecture i386
packages:
  - build-essential
  - gcc-multilib
  - gdb
  - gdbserver
  - git
  - curl
  - tmux
  - file
  - unzip
  - elfutils
  - patchelf
  - ltrace
  - strace
  - socat
  - netcat-openbsd
  - python3-dev
  - python3-setuptools
  - python3-pwntools
  - python3-capstone
  - python3-unicorn
  - python3-ropgadget
  - ruby
  - ruby-dev
  - libc6-dbg
  - libssl-dev
  - libffi-dev
  - pkg-config
  - fish
  - eza
  - bat
  - zoxide
  - ripgrep
  - fd-find
  - neovim
  - ncurses-term
  - stow
mounts:
  - [pwnshare, /home/armaan/pwn, virtiofs, "defaults,nofail", "0", "0"]
write_files:
  - path: /etc/fish/conf.d/10-pwn.fish
    content: |
      if status is-interactive; and test "$PWD" = "$HOME"; and test -d "$HOME/pwn"
          builtin cd "$HOME/pwn"
      end
  - path: /etc/default/grub.d/99-pwn.cfg
    content: |
      GRUB_CMDLINE_LINUX_DEFAULT="$GRUB_CMDLINE_LINUX_DEFAULT vsyscall=emulate"
  - path: /usr/local/bin/pwndbg
    permissions: "0755"
    content: |
      #!/bin/sh
      if grep -q pwndbg "$HOME/.gdbinit" 2>/dev/null; then
        exec gdb "$@"
      fi
      exec gdb -q -ex "source /opt/pwndbg/gdbinit.py" "$@"
  - path: /usr/local/bin/pwn-dotfiles
    permissions: "0755"
    content: |
      #!/bin/bash
      set -euo pipefail
      DOT="$HOME/dotfiles"
      export GIT_TERMINAL_PROMPT=0
      if [ -d "$DOT/.git" ]; then
        git -C "$DOT" pull --ff-only || true
      else
        git clone https://github.com/ArmaanLala/dotfiles.git "$DOT"
      fi
      if [ "${PWN_DOTFILES_SSH:-0}" = 1 ]; then
        git config --global --unset url."https://github.com/".insteadOf || true
      else
        git config --global url."https://github.com/".insteadOf "git@github.com:"
      fi
      git -C "$DOT" submodule update --init --recursive \
        || echo "note: private submodules skipped - see 'pwnbox sync -A'"
      stow -d "$DOT" -t "$HOME" --restow --adopt .
      git -C "$DOT" checkout -- . || true
  - path: /home/armaan/.gdbinit
    owner: "armaan:armaan"
    permissions: "0644"
    defer: true
    content: |
      source /opt/pwndbg/gdbinit.py
      set debuginfod enabled on
runcmd:
  - chown armaan:armaan /home/armaan
  - apt-get install -y libc6:i386 libc6-dbg:i386 zlib1g:i386
  - gem install --no-document one_gadget seccomp-tools
  - git clone --depth 1 https://github.com/pwndbg/pwndbg /opt/pwndbg
  - (cd /opt/pwndbg && ./setup.sh --update)
  - chown -R armaan:armaan /opt/pwndbg
  - update-grub
  - ln -sf /usr/bin/batcat /usr/local/bin/bat
  - ln -sf /usr/bin/fdfind /usr/local/bin/fd
  - curl -fsSL https://starship.rs/install.sh | sh -s -- --yes
  - runuser -u armaan -- /usr/local/bin/pwn-dotfiles
  - chsh -s /usr/bin/fish armaan
final_message: "pwnbox ready. Reboot once for vsyscall=emulate."
